import { describe,expect,it } from 'vitest';
import { buildPortableBundle, parsePortablePayload } from './portable';
import type { TourSnapshot } from '../../types/domain';
const now='2026-08-31T00:00:00.000Z';
const snapshot:TourSnapshot={tour:{id:'t',title:'테스트 투어',status:'draft',faceAssetId:'a',createdAt:now,updatedAt:now,lastOpenedAt:now},shops:[{id:'s',tourId:'t',name:'샵',order:0,createdAt:now,updatedAt:now}],dresses:[{id:'d',tourId:'t',shopId:'s',order:0,label:'Dress 01',topStyle:'offShoulder',neckline:'sweetheart',silhouette:'aLine',waistline:'natural',backStyle:'buttonBack',fabric:'lace',color:'ivory',train:'chapel',details:[],quickTags:['신부 픽'],memo:'좋음',isFavorite:true,faceTransform:{x:0,y:0,scale:1,rotation:0},createdAt:now,updatedAt:now}],assets:[{id:'a',tourId:'t',kind:'face',mimeType:'image/webp',blob:new Blob(['face']),width:100,height:100,byteLength:4,sha256:'0'.repeat(64),createdAt:now}]};
describe('portable payload',()=>{it('face excluded removes every reference and byte candidate',()=>{const out=buildPortableBundle(snapshot,false);expect(out.assets).toHaveLength(0);expect(out.payload.assets).toHaveLength(0);expect(out.payload.tour.faceAssetId).toBeUndefined();expect(out.payload.dresses[0].faceTransform).toBeUndefined();expect(out.payload.includeFace).toBe(false)});it('face included preserves asset ref and back style',()=>{const out=buildPortableBundle(snapshot,true);expect(out.assets).toHaveLength(1);expect(out.payload.assets[0].id).toBe('a');expect(parsePortablePayload(JSON.parse(JSON.stringify(out.payload))).dresses[0].backStyle).toBe('buttonBack')})});
