import { useState } from 'react';
import { ArrowLeft } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { createTour } from '../../db/repositories';
import { useUIStore } from '../../stores/uiStore';
const today=()=>new Date().toLocaleDateString('sv-SE');
export function NewTourPage(){const nav=useNavigate();const [brideName,setBride]=useState('');const [date,setDate]=useState(today());const [title,setTitle]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState<string>();const setSave=useUIStore(s=>s.setSaveStatus);const toast=useUIStore(s=>s.showToast);return <main className="min-h-dvh px-5 pb-28 pt-[calc(18px+env(safe-area-inset-top))]"><button className="grid h-11 w-11 place-items-center rounded-full bg-stone-50" onClick={()=>nav(-1)} aria-label="뒤로"><ArrowLeft/></button><div className="mt-10"><p className="text-xs font-semibold text-[#a75e55]">새 드레스투어</p><h1 className="mt-2 text-3xl font-black tracking-[-.04em]">오늘 기록을<br/>만들어볼까요?</h1><p className="mt-3 text-sm text-stone-400">모든 항목은 선택이에요. 바로 시작해도 됩니다.</p></div><div className="mt-9 space-y-5"><label className="block text-sm font-semibold">신부 이름 <span className="font-normal text-stone-300">선택</span><input maxLength={30} value={brideName} onChange={e=>setBride(e.target.value)} placeholder="예: 히똥" className="mt-2 h-14 w-full rounded-2xl border border-stone-200 px-4 outline-none focus:border-[#bd7469]"/></label><label className="block text-sm font-semibold">투어 날짜<input type="date" value={date} onChange={e=>setDate(e.target.value)} className="mt-2 h-14 w-full rounded-2xl border border-stone-200 px-4 outline-none focus:border-[#bd7469]"/></label><label className="block text-sm font-semibold">투어 제목 <span className="font-normal text-stone-300">선택</span><input maxLength={50} value={title} onChange={e=>setTitle(e.target.value)} placeholder="비워두면 자동으로 만들어요" className="mt-2 h-14 w-full rounded-2xl border border-stone-200 px-4 outline-none focus:border-[#bd7469]"/></label>{error&&<p role="alert" className="rounded-2xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}</div><div className="fixed bottom-0 left-1/2 w-full max-w-[480px] -translate-x-1/2 border-t border-stone-100 bg-[#fffdfa]/95 px-5 pb-[calc(16px+env(safe-area-inset-bottom))] pt-3 backdrop-blur"><button disabled={busy} className="h-14 w-full rounded-2xl bg-stone-900 font-bold text-white disabled:opacity-50" onClick={async()=>{setBusy(true);setError(undefined);setSave('saving');try{const id=await createTour({brideName,tourDate:date,title});setSave('saved');nav(`/tour/${id}`,{replace:true})}catch(e){const message=e instanceof Error?e.message:'투어를 만들지 못했어요. 다시 시도해 주세요.';setSave('error');setError(message);toast(message);setBusy(false)}}}>투어 만들기</button></div></main>}
